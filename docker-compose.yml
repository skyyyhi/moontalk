services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /mnt/volume-fsn1-1:/app/data
    environment:
      PORT: 3000
      DATABASE_PATH: /app/data/db.sqlite
      ADMIN_SECRET: ${ADMIN_SECRET}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
